{
  "name": "access-control-functions",
  "description": "F4 — the agent's governed read tools as HTTPS callable Functions (emulator-only).",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "engines": { "node": "20" },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
