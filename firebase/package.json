{
  "name": "access-control-firebase",
  "version": "0.0.0",
  "private": true,
  "description": "Firebase emulator config for the Access Control Demonstrator (Day 2). Isolated from the Day 1 Python build; nothing here touches src/ or Demo data/.",
  "scripts": {
    "emulators": "firebase emulators:start"
  },
  "devDependencies": {
    "firebase-tools": "^15.19.1"
  }
}
